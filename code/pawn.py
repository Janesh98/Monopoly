class Pawn:

    def __init__(self, colour, position):

        ''' initializes the pawn selected by the user
            and applies the selected colour
            colour : (str)
            position : (int)
        '''
        pass

    def move(self, position):

        ''' updates the position of the pawn on the board
            return (int)
        '''
        pass
