class Board:

    def __init__(self, pawn_list, chance_cards, comm_chest_cards):

        ''' initializes:
            pawn_list : list of different types of pawns (Pawn[])
            chance_cards : list of different types of chance cards (str[])
            comm_chest_cards : list of different community chest cards (str[])
        '''
        pass

    def random_card(self, card_type):

        ''' returns a random card from the list of the requested type
            return (str)
        '''
